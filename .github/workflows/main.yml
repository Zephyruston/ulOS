name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: action/checkout@v1
    - name: Run a one-line script
      run: echo Hello,world!
    - name: Run a multi-lin script
      run:
        echo Add other actions to build,
        echo test, and deploy your project.
